# התאמה לאבטחת מידע בישראל - צ'קליסט ופערים בפרויקט

המסמך הזה הוא מפת עבודה טכנית ותפעולית לפרויקט. הוא לא מחליף ייעוץ משפטי או ממונה אבטחת מידע, אבל כן מתרגם את הדרישות המקובלות בישראל לצעדים מעשיים עבור הקוד הקיים.

## 1. המסגרת הרלוונטית בישראל

למערכת כזו, שמנהלת משתמשים, תיאומי כיתות, שיוכים, לוחות זמנים וייתכן שגם נתוני תלמידים או שיוך כיתתי, רלוונטיות בדרך כלל לפחות הדרישות הבאות:

- תקנות הגנת הפרטיות (אבטחת מידע), התשע"ז-2017
- חוק הגנת הפרטיות, התשמ"א-1981
- דרישות רישום מאגר מידע, אם חלות על הגוף המפעיל ועל סוג המידע
- נהלי אבטחת מידע, הרשאות, תיעוד אירועים ותגובה לאירועי אבטחה של הארגון עצמו

הערכה מעשית: לפרויקט כזה סביר להניח שנדרש לכל הפחות משטר בקרה של `רמת אבטחה בינונית`, ובמקרים מסוימים גם יותר, למשל אם נשמר מידע רגיש, מידע על קטינים, מאגר גדול, או שיש גישה של כמה בעלי תפקידים וממשקי צד ג'.

## 2. מה התקן/הדרישות כוללות בפועל

כדי לעמוד בדרישות המקובלות בישראל, לא מספיק "להוסיף JWT" או "לשים HTTPS". בדרך כלל צריך את כל השכבות הבאות:

### ממשל ותיעוד

- מסמך הגדרות מאגר: איזה מידע נשמר, למה, מי ניגש, איפה הוא נשמר, מי ספקי המשנה
- סיווג מידע: תלמידים, אנשי צוות, משתמשי מערכת, הרשאות, לוגים
- מדיניות הרשאות וסקר גישה תקופתי
- נוהל אירועי אבטחה ודיווח
- מדיניות שמירת מידע ומחיקה
- תיעוד ספקים, ענן, גיבויים וסביבות פיתוח

### בקרות אפליקטיביות

- אימות חזק: סיסמאות חזקות, נעילת ניסיונות, MFA למנהלים
- הרשאות מבוססות תפקיד עם הפרדה אמיתית בין משתמש רגיל, רכז, מנהל
- מניעת יצירת משתמשים בעלי הרשאות גבוהות בלי אישור מנהל
- ניהול סשנים וטוקנים מאובטח
- לוגים ואודיט לפעולות רגישות
- ולידציה של קלט והקשחת API
- בקרות מול WebSocket אם יש תקשורת בזמן אמת

### תשתית ותפעול

- HTTPS בלבד
- הצפנת מידע רגיש במעבר, ובמקרים מסוימים גם במנוחה
- ניהול סודות מחוץ לקוד וללא סודות חלשים
- גיבויים, שחזור, בדיקות שחזור
- סריקות חולשות ותלויות
- הפרדת dev / test / production
- הרשאות מינימליות למסד הנתונים

## 3. פערים שמצאתי כרגע בפרויקט

### פערים קריטיים

1. רישום משתמש פתוח עם בחירת תפקיד מהבקשה

קובץ:
`src/backend/api/routes/auth.ts`

הנתיב `/api/auth/register` פתוח, ומאפשר לשלוח `role` מהלקוח. בפועל אפשר לנסות ליצור משתמש עם תפקיד גבוה יותר.

מה צריך:
- לבטל הרשמה פתוחה בפרודקשן
- לאפשר יצירת משתמשים רק על ידי מנהל
- לייצר allowlist של תפקידים שמותר להקצות, ורק לפי הרשאות

2. יצירת bridge token בלי אימות מספק

קבצים:
- `src/backend/api/routes/auth.ts`
- `src/frontend/src/app/api/auth/bridge-token/route.ts`

הנתיב `/api/auth/bridge-token` אינו מוגן ב־`authMiddleware`. בפרודקשן הוא עשוי להנפיק JWT עבור משתמש קיים רק לפי `id/email/role`, וב־development ההתנהגות אפילו מקלה יותר.

מה צריך:
- לחסום את הנתיב הזה לגמרי אם אינו הכרחי
- אם הוא הכרחי: לדרוש משתמש מחובר, חתימה פנימית, allowlist של מקור, ובדיקת התאמה בין session קיים לבין המשתמש המבוקש
- לרשום audit מלא על כל הנפקת token

3. שמירת טוקנים ב־`localStorage` וב־`sessionStorage`

קבצים:
- `src/frontend/src/contexts/AuthContext.tsx`
- `src/frontend/src/lib/api.ts`
- `src/frontend/src/lib/auth-backend-bridge.ts`

טוקנים בדפדפן ב־storage חשופים יותר ל־XSS. עבור מערכת עם מידע ארגוני/חינוכי זה לא מצב טוב.

מה צריך:
- לעבור ל־`HttpOnly Secure SameSite` cookies
- להוסיף מנגנון refresh / rotation
- לקצר חיי access token

4. WebSocket ללא אימות משתמש

קובץ:
`src/backend/index.ts`

חיבורי socket מתקבלים ללא בדיקת זהות או הרשאה, ומשתמש יכול להצטרף ל"חדרים" בלי בקרה.

מה צריך:
- לאמת socket בזמן handshake
- לבדוק הרשאות לפני `join-room`
- להוסיף audit לאירועי חיבור והרשאה

### פערים גבוהים

5. `authLimiter` קיים אבל לא מופעל על נתיבי auth

קבצים:
- `src/backend/middleware/rateLimiter.ts`
- `src/backend/api/routes/auth.ts`

יש rate limiter ייעודי לאימות, אבל הוא לא מחובר בפועל לנתיבי login/register/bridge-token.

מה צריך:
- להחיל `authLimiter` על login
- להקשיח במיוחד register, reset password, bridge-token
- להוסיף lockout/slowdown לפי משתמש ו־IP

6. אין מדיניות סיסמאות, MFA או נעילה לאחר ניסיונות כושלים

מה צריך:
- אורך מינימלי, חסימת סיסמאות חלשות
- שינוי סיסמה ראשוני / איפוס מאובטח
- MFA לפחות למנהלים ולרכזים
- תיעוד ניסיונות התחברות כושלים

7. לוגים אינם שקולים ל־audit trail מלא

קבצים:
- `src/backend/utils/logger.ts`
- `src/backend/infrastructure/database/migrations/20260413_001_create_live_schema_baseline.ts`

יש טבלת `audit_logs`, אבל מהקוד האפליקטיבי לא נראה שיש מנגנון עקבי ומחייב שכותב אליה את כל הפעולות הרגישות.

מה צריך:
- audit מסודר לכניסות, כשלי כניסה, שינויי הרשאות, יצירת משתמש, איפוס סיסמה, שינויים בהקצאות ו־override
- שמירת `who`, `when`, `from where`, `what changed`
- מניעת מחיקה/שינוי של audit רגיל על ידי משתמשים אפליקטיביים

8. אין הוכחה להקשחת סודות וקונפיגורציה לפרודקשן

קבצים:
- `.env`
- `.env.example`

יש תלות ב־`JWT_SECRET` ובסודות נוספים, אך כרגע אין מנגנון שמכריח סוד חזק, rotation, או ניהול דרך vault/secret manager.

מה צריך:
- לאסור על default secrets
- בדיקת startup שמפילה את השרת אם סוד חלש או חסר
- ניהול סודות דרך secret manager בסביבת פרודקשן

### פערים בינוניים

9. חסר תיעוד פורמלי של מאגרי מידע, הרשאות, שמירת מידע ומחיקה

זה לא רק עניין תכנותי. בתקינה הישראלית זה חלק מהציות.

מה צריך:
- מסמך מאגר
- מטרות שימוש
- רשימת תפקידים בעלי גישה
- retention schedule
- נוהל מחיקה / אנונימיזציה

10. חסרה הקשחת הרשאות ברמת נתונים

מה צריך:
- בדיקה שכל coordinator רואה ומשנה רק את השכבה/הקבוצה שלו
- מניעת horizontal privilege escalation
- בדיקות הרשאה ברמת רשומה, לא רק ברמת route

11. חסרות בדיקות אבטחה אוטומטיות

מה צריך:
- dependency scanning
- SAST
- בדיקות authz/authn
- בדיקות brute force ו־token misuse

## 4. מה צריך להוסיף לפרויקט בפועל

### בקוד

- חסימת `register` פתוח או הפיכתו ל־admin only
- חסימת `bridge-token` או כתיבה מחדש עם אימות חזק
- מעבר מטוקן ב־storage לעוגיות `HttpOnly`
- middleware לאודיט אחיד לפעולות רגישות
- אימות WebSocket
- הפעלת `authLimiter`
- מדיניות סיסמאות ונעילת ניסיונות
- בדיקות הרשאה ברמת רשומה
- בדיקות startup לסודות חלשים
- הוספת security headers ו־CORS קשיחים גם בפרונט וגם בבקאנד

### במסד הנתונים

- טבלת audit מחייבת ומאוישת בפועל
- שדות `last_login`, `failed_login_attempts`, `locked_until`, `password_changed_at`
- תמיכה ב־refresh tokens אם בוחרים מודל כזה
- מדיניות retention ללוגים ולנתונים
- הצפנת שדות רגישים במיוחד אם קיימים

### בתפעול ותיעוד

- מסמך אפיון מאגר ומפת זרימת מידע
- נוהל ניהול הרשאות
- נוהל אירוע אבטחה
- נוהל גיבוי ושחזור
- רישום פעולות מנהל
- בדיקות חדירה או לפחות סקר יישומי לפני עלייה לאוויר

## 5. סדר עדיפויות מומלץ

### שלב 1 - לעצור סיכונים מיידיים

- לסגור `register`
- לסגור `bridge-token`
- להפעיל `authLimiter`
- לבטל טוקנים ב־`localStorage`
- להוסיף אימות ל־WebSocket

### שלב 2 - לעמוד בבקרות הליבה

- audit trail מלא
- password policy + MFA
- בדיקות הרשאה ברמת מידע
- הקשחת secrets ו־production config

### שלב 3 - ציות ותפעול

- מסמך מאגר
- מדיניות retention
- נוהל אירועי אבטחה
- סקר סיכונים ובדיקות תקופתיות

## 6. מסקנה קצרה

במצב הנוכחי הפרויקט כולל כמה רכיבי אבטחה טובים, אבל הוא עדיין לא במצב שאפשר להציג כ"תואם לדרישות אבטחת מידע בישראל" לפרויקט חינוכי/ארגוני. במיוחד צריך לטפל מיד ב־register, bridge-token, token storage, audit, והרשאות.

## 7. הצעת המשך

השלב הטכני הנכון הבא הוא לבצע hardening ממוקד של שכבת האימות וההרשאות, ואז לבנות מסמך ציות קצר שממפה:

- דרישה רגולטורית
- בקרה נדרשת
- סטטוס נוכחי
- שינוי שבוצע
- פערים שנותרו
